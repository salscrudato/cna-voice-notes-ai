# ============================================================================
# CNA Voice Notes AI - Environment Configuration
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control
# ============================================================================

# ============================================================================
# CHAT PROVIDER CONFIGURATION
# ============================================================================
# Choose between two modes:
# 1. openai-direct: Use OpenAI API directly from the browser (development)
# 2. proxied: Use a backend proxy for better security (production recommended)

# Chat provider mode (default: openai-direct)
# VITE_CHAT_PROVIDER=openai-direct

# ============================================================================
# OPENAI DIRECT MODE (Development)
# ============================================================================
# Required when VITE_CHAT_PROVIDER=openai-direct
# Get your API key from: https://platform.openai.com/api-keys
# Model: gpt-4o-mini (fast, cheap, and high-quality)
VITE_OPENAI_API_KEY=sk-your-api-key-here

# ============================================================================
# PROXIED MODE (Production Recommended)
# ============================================================================
# Required when VITE_CHAT_PROVIDER=proxied
# Set this to your backend proxy endpoint (e.g., Cloud Run, Cloud Functions)
# Example: https://my-proxy.cloudfunctions.net/chat
# VITE_CHAT_PROXY_URL=https://your-proxy-endpoint.com/chat

# Optional: Enable streaming responses from proxy (if supported)
# VITE_CHAT_STREAMING_ENABLED=false

# ============================================================================
# FIREBASE CONFIGURATION
# ============================================================================
# By default, the app uses the 'generic-voice' Firebase project
# Uncomment and fill in these values to override the default configuration
# All fields are optional - if not set, defaults will be used

# VITE_FIREBASE_API_KEY=your-firebase-api-key
# VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-project-id
# VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
# VITE_FIREBASE_APP_ID=your-app-id
# VITE_FIREBASE_MEASUREMENT_ID=your-measurement-id

# ============================================================================
# Setup Instructions
# ============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Choose your chat provider mode:
#    - For development: Set VITE_OPENAI_API_KEY from https://platform.openai.com/api-keys
#    - For production: Set VITE_CHAT_PROVIDER=proxied and VITE_CHAT_PROXY_URL
# 3. (Optional) Override Firebase configuration if using a custom project
# 4. Run: npm run dev
# ============================================================================
